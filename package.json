{
  "name": "@yahapi/uid",
  "version": "0.1.1",
  "description": "Generates unique ids",
  "main": "dist/index.js",
  "author": "Niels Krijger <niels@kryger.nl>",
  "license": "ISC",
  "repository" : {
    "type" : "git",
    "url" : "https://github.com/yahapi/uid.git"
  },
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-core": "^6.10.4",
    "babel-preset-es2015": "^6.9.0"
  },
  "scripts": {
    "build": "rm -rf ./dist && babel src --out-dir dist",
    "prepublish": "npm run build"
  }
}
